---
export interface Props {
    title: string;
}

const { title } = Astro.props;
---

<h2 class="w-full ml-4 mb-2 max-w-5xl text-xl font-bold">{title}</h2>
<div class="flex flex-wrap max-w-5xl w-full">
    <slot />
</div>